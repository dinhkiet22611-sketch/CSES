#include <bits/stdc++.h>
using namespace std;
#define ll long long 
ll n,a[1000005];
int main()
{
    cin >> n;
    if(n == 1) {cout << 1;return 0;}
    if(n<= 3) {cout << "NO SOLUTION";return 0;}
    for(ll i = 2; i <= n;i+=2) 
    {
        cout << i << ' ';
    }
    for(ll i = 1; i <= n;i+=2) cout << i << ' ';
}
